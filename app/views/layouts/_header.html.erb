

<nav class="bg-white border-gray-200 dark:bg-gray-900">
  <div class="flex flex-wrap justify-between items-center mx-auto max-w-screen-xl p-4">
    <a href="#" class="flex items-center space-x-3 rtl:space-x-reverse">
      <img src="https://flowbite.com/docs/images/logo.svg" class="h-8" alt="Flowbite Logo" />
      <span class="self-center text-2xl font-semibold whitespace-nowrap dark:text-white">LoanLender</span>
    </a>

    <div class="flex items-center space-x-6 rtl:space-x-reverse">
      <% if user_signed_in? %>
        
        <%= button_to "Logout", destroy_user_session_path, method: :delete, class: "text-sm text-blue-600 dark:text-blue-500 hover:underline bg-transparent border-none p-0", form: { class: "inline" } %>
      <% else %>
        <a href="<%= new_user_session_path %>" class="text-sm text-blue-600 dark:text-blue-500 hover:underline">Login</a>
      <% end %>
    </div>
  </div>
</nav>

<nav class="bg-gray-50 dark:bg-gray-700">
    <div class="max-w-screen-xl px-4 py-3 mx-auto">
        <div class="flex items-center">
            <ul class="flex flex-row font-medium mt-0 space-x-8 rtl:space-x-reverse text-sm">
                <li>
                    <a href="#" class="text-gray-900 dark:text-white hover:underline" aria-current="page">Home</a>
                </li>
                <% if user_signed_in? %>
                    <% if current_user.admin? %>
                        <li>
                            <a href="<%= admin_loans_path %>" class="text-gray-900 dark:text-white hover:underline">Admin</a>
                        </li>
                    <% else %>
                        <li>
                            <a href="<%= user_loans_path %>" class="text-gray-900 dark:text-white hover:underline">User</a>
                        </li>
                    <% end %>
                <% end %>
            </ul>
        </div>
    </div>
</nav>
